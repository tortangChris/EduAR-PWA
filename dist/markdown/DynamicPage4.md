# 3D Arrays (Cube Representation)

- **3D arrays** = arrays with **3 dimensions** (rows, columns, depth).
- Example:
  ```text
  arr[x][y][z]
  ```
- Often used in **3D graphics, simulations, voxel storage**.
